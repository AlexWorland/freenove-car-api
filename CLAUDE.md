# Project Instructions

## Sensor Reads
- Always capture a camera image when reading the ultrasonic sensor. Ultrasonic alone is unreliable in cluttered environments — the camera provides ground truth for what the sensor is actually pointed at.
